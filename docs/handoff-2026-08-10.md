# Handoff - 2026-08-10

## Completed in this slice

- Local/dev host wiring and route hardening for API, Web, and AgentGateway.
- AgentGateway now exposes typed `POST /agents/booking/check-availability` and forwards to API `/availability`.
- Azure dev infrastructure Terraform expanded for Container Apps + supporting resources.
- CI workflow now deploys infra + app containers.
- Foundry stage upgraded from placeholder/package-only to a real deployment job that creates a prompt-agent version in Foundry.

## Foundry deployment implementation

- Workflow job: `.github/workflows/dev-deploy.yml` (`deploy-foundry-agent`).
- Deployment script: `foundry/scripts/deploy_prompt_agent.py`.
- Manifest retained: `foundry/agent.manifest.yaml` (validated by workflow).
- Deployment metadata output artifact: `foundry/foundry-deployment-result.json`.

## Required GitHub repository secrets

- `AZURE_CLIENT_ID`
- `AZURE_TENANT_ID`
- `AZURE_SUBSCRIPTION_ID`
- `FOUNDRY_PROJECT_ENDPOINT`
- `FOUNDRY_MODEL_DEPLOYMENT_NAME`
- `FOUNDRY_AGENT_INSTRUCTIONS` (optional)
- `CLINICFLOW_API_BASE_URL` (optional)
- `CLINICFLOW_GATEWAY_BASE_URL` (optional)

## Verification status

- `dotnet build ClinicFlowAi.sln` passed earlier in session.
- `dotnet test ClinicFlowAi.sln` passed earlier in session (13 tests).
- `terraform -chdir=infra/terraform validate` passed earlier in session.
- Workflow YAML parses successfully.
- Foundry deployment script compiles (`python -m py_compile`).

## Notes for next session

- Run a real pipeline execution to verify Foundry deployment with live secrets and RBAC.
- Optional hardening:
  - Add Foundry post-deploy smoke invocation to CI.
  - Add richer CI failure diagnostics for Foundry deploy step.
  - Make Foundry agent name/temperature configurable via workflow inputs.

## Branch/PR intention

- Changes are prepared to be raised as a draft PR into `main`.
